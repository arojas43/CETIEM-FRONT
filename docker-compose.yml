# ============================================================
# SECRETARIA DE ECONOMIA — Stack completo en Docker
#
# Inicio rápido para desarrolladores:
#   1. cp .env.example .env
#   2. Edita .env y agrega tus NVIDIA_API_KEY y NVIDIA_QA_API_KEY
#   3. docker compose up --build
#
# URLs:
#   http://localhost:3000  → Aplicación
#   http://localhost:8001  → OpenKB API (Q&A + KB wiki por empresa)
#   localhost:5434         → PostgreSQL (cliente externo / Prisma Studio)
#   localhost:6381         → Redis (depuración)
#   http://localhost:5555  → Prisma Studio (npx prisma studio --port 5555)
# ============================================================

# Variables de entorno internas compartidas por app y workers.
# NOTA: Las claves NVIDIA_* NO van aquí — vienen directo del .env via env_file
# para evitar que el shell del host las sobreescriba con valores obsoletos.
x-app-env:
  # ── Base de datos ─────────────────────────────────────────
  # Usa el hostname del servicio Docker, no localhost
  &app-env
  DATABASE_URL: "postgresql://postgres:secretaria-economia@postgres:5432/economia_db?schema=certificacion_ia"

  # ── Auth ──────────────────────────────────────────────────
  NEXTAUTH_URL: "http://localhost:3000"
  NEXT_PUBLIC_APP_URL: "http://localhost:3000"
  NEXTAUTH_SECRET: "${NEXTAUTH_SECRET:-secretaria-economia-dev-secret}"
  AUTH_SECRET: "${AUTH_SECRET:-secretaria-economia-dev-secret}"
  AUTH_TRUST_HOST: "true"

  # ── Almacenamiento ────────────────────────────────────────
  LOCAL_STORAGE_PATH: "/app/uploads"

  # ── Redis (hostname del servicio Docker) ──────────────────
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"

  # ── PageIndex ─────────────────────────────────────────────
  PAGEINDEX_LOCAL_MODE: "true"

  # ── OpenKB (KB wiki por empresa, Q&A sin vector DB) ──────
  OPENKB_SERVICE_URL: "http://openkb-api:8000"

  # ── Límites ───────────────────────────────────────────────
  MAX_CHUNKS_TO_PROCESS: "500"
  NODE_ENV: "production"
  RATE_LIMIT_DISABLED: "${RATE_LIMIT_DISABLED:-false}"

services:

  # ── PostgreSQL ─────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: secretaria-economia-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secretaria-economia
      POSTGRES_DB: economia_db
    ports:
      - "5434:5432" # 5434 evita conflicto con Postgres local en 5432/5433
    volumes:
      - economia_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 10

  # ── Redis (BullMQ) ─────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: secretaria-economia-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - "6381:6379" # 6381 evita conflicto con Redis local en 6379
    volumes:
      - economia_redis_data:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 10

  # ── DB Init (migración + seed, se ejecuta una vez y sale) ──
  db-init:
    build: .
    container_name: secretaria-economia-db-init
    command: >
      sh -c "
        echo '→ Aplicando schema Prisma...' &&
        npx prisma db push &&
        echo '→ Ejecutando seed (usuarios de prueba)...' &&
        npx prisma db seed &&
        echo '✓ Base de datos lista'
      "
    environment:
      DATABASE_URL: "postgresql://postgres:secretaria-economia@postgres:5432/economia_db?schema=certificacion_ia"
      NODE_ENV: "production"
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  # ── Aplicación Next.js ─────────────────────────────────────
  app:
    build: .
    container_name: secretaria-economia-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file: .env        # NVIDIA_* y otras keys desde .env (sin riesgo de override del shell)
    environment:
      <<: *app-env         # Sobreescribe variables Docker-internas (DB host, Redis host, etc.)
    volumes:
      - ./src:/app/src
      - ./prisma:/app/prisma
      - ./public:/app/public
      - ./package.json:/app/package.json
      - ./next.config.js:/app/next.config.js
      - ./postcss.config.js:/app/postcss.config.js
      - ./tailwind.config.js:/app/tailwind.config.js
      - ./tsconfig.json:/app/tsconfig.json
      - economia_uploads:/app/uploads
    command: npm run start
    depends_on:
      db-init:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      openkb-api:
        condition: service_healthy

  # ── OpenKB Knowledge API ──────────────────────────────────
  openkb-api:
    build: ./openkb-api
    container_name: secretaria-economia-openkb
    restart: unless-stopped
    ports:
      - "8001:8000"
    env_file: .env        # lee todas las vars del .env primero
    environment:
      OPENKB_DATA_DIR: "/data/kb"
      NVIDIA_API_BASE: "https://integrate.api.nvidia.com/v1"
      # Usar KEY3 (NVIDIA_INTENT_API_KEY) para OpenKB — separa el budget de rate-limit
      # del KEY1 que usan los workers y el pipeline de Q&A de la app.
      NVIDIA_API_KEY: "${NVIDIA_INTENT_API_KEY}"
    volumes:
      - economia_openkb_data:/data/kb
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health', timeout=3)"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s

  # ── Workers BullMQ (procesamiento de documentos en background)
  workers:
    build: .
    container_name: secretaria-economia-workers
    command: npm run workers
    restart: unless-stopped
    env_file: .env        # NVIDIA_* keys desde .env
    environment:
      <<: *app-env         # Sobreescribe variables Docker-internas
    volumes:
      - ./src:/app/src
      - economia_uploads:/app/uploads
    depends_on:
      db-init:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      openkb-api:
        condition: service_healthy

volumes:
  economia_postgres_data:
  economia_redis_data:
  economia_uploads:
  economia_openkb_data:
